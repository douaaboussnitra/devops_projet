apiVersion: v1
kind: Service
metadata:
  name: devops-app-service
spec:
  type: NodePort
  selector:
    app: devops-app
  ports:
    - port: 80          # Port inside the cluster
      targetPort: 5000  # Port your container exposes
      nodePort: 30080   # Port exposed on your EC2 node
