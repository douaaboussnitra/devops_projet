name: CI/CD DevOps Pipeline

on:
  push:
    branches: [main]

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: pytest test_app.py -v

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/login-action@v2
        with:
          username: \${{ douaabousnitra }}
          password: \${{ dckr_pat__LFsjglxzE1lYDXOgJVIxu5Qy60 }}
      - run: |
          docker build -t \${{ secrets.DOCKER_USERNAME }}/devops-app:latest .
          docker push \${{ secrets.DOCKER_USERNAME }}/devops-app:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: \${{ secrets.SERVER_IP }}
          username: ubuntu
          key: \${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/devops-app
            kubectl apply -f k3s/deployment.yaml
            kubectl apply -f k3s/service.yaml
            kubectl rollout status deployment/devops-app
